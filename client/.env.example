# Public API URL (browser + build). No trailing slash.
# Example: https://haf-api.onrender.com
VITE_API_BASE_URL=

# SSR fetches during server render (Docker/Render: same as API URL).
VITE_API_SSR_URL=
